try:
    from qfluentwidgets import HeroTitleLabel
    print("HeroTitleLabel exists")
except ImportError:
    print("HeroTitleLabel does not exist")

try:
    from qfluentwidgets import LargeTitleLabel
    print("LargeTitleLabel exists")
except ImportError:
    print("LargeTitleLabel does not exist")

try:
    from qfluentwidgets import TitleLabel
    print("TitleLabel exists")
except ImportError:
    print("TitleLabel does not exist")

try:
    from qfluentwidgets import SubtitleLabel
    print("SubtitleLabel exists")
except ImportError:
    print("SubtitleLabel does not exist")
